import React from 'react'

const Default = ({title}) => {
  return (
    <div className='default' style={{display: 'flex', justifyContent:'center', width:  '100%'}}>
        {title}
    </div>
  )
}

export default Default